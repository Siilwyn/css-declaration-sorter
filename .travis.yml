language: node_js

node_js:
  - 10
  - 8

script:
  - npm run test:ci

branches:
  only:
    master

deploy:
  provider: npm
  email: "talk@selwyn.cc"
  api_key: $NPM_TOKEN
  on:
    branch: master
    tags: true

notifications:
  email: false
